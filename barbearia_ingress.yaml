apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: barbearia-ingress
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: svc-barbearia
          servicePort: 80